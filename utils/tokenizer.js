// const jwt = require("jsonwebtoken");
// const secret = "1234yhubs9538b";

// //Encrypt Data

// export const encryptData = (datum) => {
//   const token = jwt.sign(datum, secret); //sign jwt token

//   return token;
// };

// //decrypt data
// export const decryptedData = (token) => {
//   let data = jwt.verify(token, secret);
//   return data;
// };
